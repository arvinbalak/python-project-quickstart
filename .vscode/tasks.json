{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Current Python file: Read input.txt and Run",
            "type": "process",
            // "command": "python ${file} < ${fileDirname}/input.txt",
            "command": "${config:python.pythonPath}",
            "args": [
                "${file}"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}